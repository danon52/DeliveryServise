interface Product {
  id: number;
  photoUrl: string
  title: string;
  composition: string;
  price: number;
}

interface newProduct {
  photoUrl: string
  title: string;
  composition: string;
  price: number;
}